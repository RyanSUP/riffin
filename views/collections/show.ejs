<%- include('../partials/html-head') %>
<link rel="stylesheet" href="/stylesheets/tablature-styles.css" />
<%- include('../partials/nav') %>

<main>
    <div class="flex margin-lg">
        <h1 class="thin-font"><%= collection.name %></h1>

        <!-- Delete -->
        <form class="center-self" action="/collections/<%= collection._id %>?_method=DELETE" method="POST">
            <button type="submit" class="btn dlt-btn color-3 md">
                <i class="fa-solid fa-circle-minus"></i>
            </button>  
        </form>
    </div>

    <!-- View collections -->
    <div class="flex wrap space-around">
        <% tabs.forEach(tab => { %>
            <!-- Gallery element will need to be a grid -->
            <div class="flex-col md">
                <%- include('../partials/tab-card.ejs', {tab, showOwner: false}) %>
            </div>
        <% }) %>
    </div>
</main>

<%- include('../partials/footer') %>